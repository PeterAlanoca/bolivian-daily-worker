name: Local
variables:
  - name: openRouterBaseUrl
    value: https://openrouter.ai/api/v1
    enabled: true
    secret: false
    description: OpenRouter base URL (SaaS, same across all environments)
  - name: openRouterApiKey
    value: '{{process.env.OPENROUTER_API_KEY}}'
    enabled: true
    secret: false
    description: OpenRouter API key (resolved from .env, gitignored)
  - name: nvidiaBaseUrl
    value: https://integrate.api.nvidia.com/v1
    enabled: true
    secret: false
    description: NVIDIA NIM API base URL (SaaS, same across all environments)
  - name: nvidiaApiKey
    value: '{{process.env.NVIDIA_API_KEY}}'
    enabled: true
    secret: false
    description: NVIDIA API key (resolved from system environment variable, gitignored)
  - name: tokenRouterBaseUrl
    value: https://api.tokenrouter.com/v1
    enabled: true
    secret: false
    description: TokenRouter base URL (SaaS, same across all environments)
  - name: tokenRouterApiKey
    value: '{{process.env.TOKENROUTER_API_KEY}}'
    enabled: true
    secret: false
    description: TokenRouter API key (resolved from .env, gitignored)
  - name: googleBaseUrl
    value: https://generativelanguage.googleapis.com/v1beta
    enabled: true
    secret: false
    description: Google Gemini API base URL (SaaS, same across all environments)
  - name: googleApiKey
    value: '{{process.env.GEMINI_API_KEY}}'
    enabled: true
    secret: false
    description: Google Gemini API key (resolved from .env, gitignored)
  - name: groqBaseUrl
    value: https://api.groq.com/openai/v1
    enabled: true
    secret: false
    description: Groq API base URL (SaaS, same across all environments)
  - name: groqApiKey
    value: '{{process.env.GROQ_API_KEY}}'
    enabled: true
    secret: false
    description: Groq API key (resolved from .env, gitignored)
  - name: deepseekBaseUrl
    value: https://api.deepseek.com/v1
    enabled: true
    secret: false
    description: DeepSeek API base URL (SaaS, same across all environments)
  - name: deepseekApiKey
    value: '{{process.env.DEEPSEEK_API_KEY}}'
    enabled: true
    secret: false
    description: DeepSeek API key (resolved from .env, gitignored)
  - name: apiBaseUrl
    value: https://0240-2800-320-c952-1400-25e2-358a-c9f1-a370.ngrok-free.app
    enabled: true
    secret: false
    description: BolivianDaily API base URL (dummy)
  - name: loginUsername
    value: peter
    enabled: true
    secret: false
    description: Login username (dummy)
  - name: loginPassword
    value: '123456'
    enabled: true
    secret: false
    description: Login password (dummy)
  - name: accessToken
    value: ''
    enabled: true
    secret: false
    description: Access JWT; populated automatically when Login runs
  - name: refreshToken
    value: ''
    enabled: true
    secret: false
    description: Refresh JWT; populated automatically when Login runs
